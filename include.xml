<?xml version="1.0" encoding="utf-8"?>
<project>
	
	<ndll name="extension_document_picker" if="ios"/>
	
	<!-- Use the following for an Android Java extension, not needed otherwise -->
	
	<dependency name="extension_document_picker" path="dependencies/android" if="android" />
	<android extension="org.haxe.extension.DocumentPickerExtension" />
	
</project>